# BLOCKERS / 보류 (2026-06-18)

야간 자율 작업 중 **임의로 결정/진행하지 않은** 항목. 박성혁 확인 후 진행.

## 1. 경매 A·B — 발급 중복 정책 (구조 결정 필요)
- **현상:** 답사지 발급 시 `auction_property.sheet_id`를 무조건 덮어씀(`survey-pdf/route.tsx:66`).
  같은 pending 물건을 전체+지역 두 번 뽑으면 ① 먼저 종이의 fill_rest "나머지" 집합에서 빠지고
  ② 두 종이가 같은 물건을 동시에 담아 중복발송됨.
- **왜 보류:** 해결책이 두 갈래라 비즈니스 결정이 필요 — 이전에 질문했으나 미응답.
  - (a) **중복 발급 자체 차단**: 이미 열린 발급에 든 pending 물건은 새 답사지에서 제외 (단순)
  - (b) **연결 테이블** `auction_sheet_item(sheet_id, property_id)`: 한 물건이 여러 발급에 각자 공존 (견고, 범위 큼)
- **추천:** (a) 먼저. 현장에 두 팀이 같은 집 안 가게.

## 2. 경매 D — 전역 매칭 오타 무경고
- 일반 일괄입력이 `property_no` 전역 매칭(`survey-actions.ts`)이라 오타 번호도 실재하면 엉뚱한 물건 처리.
- **보류 이유:** 박성혁 "번호 오타 걱정 안 함"(2026-06-18). 필요 시 발급선택+소속검증/주소 미리보기로 보완 가능.

## 3. 알림 실발송 — 외부 키 필요
- `notify/adapters/kakao.ts`,`sms.ts` 전부 stub. 코드는 준비됨, **키만 필요**.
- 카카오 비즈메시지(채널+발신프로필+API키) 또는 NCP SENS(키 4종). → 박성혁/부장님 액션.

## 4. 운영 환경변수 확인 (Vercel) — 사람만 가능
- `CRON_SECRET`: 이제 크론 4종 모두 fail-closed. **미설정 시 자동화 전부 멈춤** → 반드시 설정 확인.
- `PII_ENCRYPTION_KEY`: 미설정 시 계좌·주민번호 평문 폴백. 운영 키 확인.

## 5. 관리자 대형 작업 (합의 후)
- 리스트 검색·페이지네이션(rent·complaints·inquiries·leases·tenants·properties), 일괄 내보내기,
  `confirm()`→공용 ConfirmDialog 통일, 상태라벨 상수 통합.
- 범위 크고 화면 회귀위험 → 우선순위·범위 합의 후 진행 권장.
